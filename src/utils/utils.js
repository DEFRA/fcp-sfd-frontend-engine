import { formatValidationErrors } from './format-validation-errors.js'
import { validateDetailsService } from './validate-details-service.js'
import { formatDob } from './format-dob.js'

export const utils = {
  formatValidationErrors,
  validateDetailsService,
  formatDob
}
